<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flyimg.dao.FileListMapper">

    <sql id="TABLE">file_list</sql>

    <!-- 查询-->
    <select id="selectimg" parameterType="com.flyimg.pojo.FileOss" resultType="com.flyimg.pojo.FileOss">
        SELECT
        *
        FROM
        <include refid="TABLE"/> a
        LEFT JOIN user b ON a.userid = b.id
        where
        1
        <if test="starttime != null">
            and
            a.updatetime &gt;= #{starttime}
        </if>
        <if test="stoptime != null">
            and
            a.updatetime &lt;= #{stoptime}
        </if>

        <if test="userid != null">
            <choose>
                <when test="selecttype != null">
                    and
                    a.userid not in (#{userid})
                </when>
                <otherwise>
                    and
                    a.userid = #{userid}
                </otherwise>
            </choose>
        </if>
        order by a.id desc
    </select>
    <!-- 此用户下总数 -->
    <select id="countimg" parameterType="integer" resultType="integer">
        select count(*) from <include refid="TABLE"/> where userid = #{userid}
    </select>

    <delete id="deleimg" parameterType="integer">
        DELETE FROM <include refid="TABLE"/> WHERE id=#{id}
    </delete>


    <select id="selectByPrimaryKey" parameterType="integer" resultType="com.flyimg.pojo.FileOss">
        select * from <include refid="TABLE"/> where id = #{id}
    </select>

    <select id="counts" resultType="integer">
        select count(*) from <include refid="TABLE"/> where 1=1
        <if test="userid != null">
            userid=#{userid}
        </if>
    </select>

    <!-- 根据图片名字，修改该对象中的其他字段属性 -->
    <update id="updateByFilename" parameterType="com.flyimg.pojo.FileOss">
         UPDATE <include refid="TABLE"/>
             <set>
                 <if test="abnormal!=null">
                     abnormal=#{abnormal}
                 </if>
                 <if test="explains!=null">
                     explains=#{explains}
                 </if>
             </set>
              where filename=#{filename}
    </update>

    <!-- 非法图片删除他  根据名称-->
    <delete id="deleteByFilename" parameterType="string">
        DELETE FROM <include refid="TABLE"/> WHERE filename =#{filename}
    </delete>

<!--    批量删除-->
    <delete id="deleAll" parameterType="integer">
        DELETE FROM <include refid="TABLE"/> WHERE id = #{id}
    </delete>

    <select id="selectListFromTime" parameterType="string" resultType="com.flyimg.pojo.FileOss">
        SELECT * from  <include refid="TABLE"/> WHERE  updated_time >= #{startTime}
    </select>
<!--查询指定用户总内存-->
    <select id="selectMoneyUsed" parameterType="integer" resultType="integer">
        SELECT sum(sizes) as sizes FROM <include refid="TABLE"/> where userid = #{userid}
    </select>

    <select id="md5Count" resultType="integer">
        select count(*) from <include refid="TABLE"/>
        <where>
            1=1
            <if test="md5key != null">
                and md5=#{md5}
            </if>
        </where>

    </select>


    <select id="selectUriByMd5" parameterType="string" resultType="com.flyimg.pojo.FileOss">
        select filename,uri from <include refid="TABLE"/> where md5 = #{md5}
    </select>

</mapper>